<%= link_to "Back to all movies", movies_path %></br>
<%= link_to image_tag(@movie.poster_image_url), movie_path(@movie)%>
<h2><%= @movie.title%>(<%= link_to "Edit", edit_movie_path(@movie) %>)</h2>
<h3>Released on <%= @movie.release_date %></h3>
<h4> Dir. <%= @movie.director %></h4>
<p><%= @movie.description %></p>
